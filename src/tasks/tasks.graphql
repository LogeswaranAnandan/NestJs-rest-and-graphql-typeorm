type Task {
  id: ID!
  title: String
  description: String
  status: TaskStatus
  user: User
}

input TaskRequestDto {
  title: String
  description: String
  status: TaskStatus
}

enum TaskStatus {
  OPEN
  IN_PROGRESS
  DONE
}

type Query {
  tasks: [Task]!
  task(id: String!): Task!
}

type Mutation {
  createTask(taskRequestDto: TaskRequestDto!): Task!
  updateTask(id: String!, taskRequestDto: TaskRequestDto!): Task!
  deleteTask(id: String!): String!
}
